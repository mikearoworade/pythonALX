# Write a function that returns the number of keys in a dictionary.
#
# Prototype: def number_keys(a_dictionary):

def number_keys(a_dictionary):
    return len(a_dictionary)  # Using len() to count the number of keys
